specVersion: v0.1.0
package:
  name: conditionaltokens
  version: v0.1.0
  url: https://github.com/pinax-network/substreams-polymarket
  description: ConditionalTokens events substreams package
  image: ../image.png

protobuf:
  files:
    - conditionaltokens.proto
  importPaths:
    - ../proto/v1

binaries:
  default:
    type: wasm/rust-v1
    file: ../target/wasm32-unknown-unknown/release/conditionaltokens.wasm

modules:
  - name: map_events
    kind: map
    inputs:
      - params: string
      - source: sf.ethereum.type.v2.Block
    output:
      type: proto:conditionaltokens.v1.Events

network: polygon
