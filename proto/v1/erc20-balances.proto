syntax = "proto3";

package erc20.balances.v1;

message Events { repeated Balance balances = 1; }

message Balance {
  optional bytes contract = 1; // log.address (`None` for native balances)
  bytes address = 2;
  string balance = 3; // balanceOf(address account) -> uint256
}
